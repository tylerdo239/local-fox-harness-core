node_modules/
lib/
!apps/web/lib/
!apps/web/lib/**
.next/
out/
*.tsbuildinfo
/data/
/.dev-state/
# Real credentials (OPENAI_API_KEY etc., see .env.example) — never commit.
/.env
# Saved browser logins and cookie exports — a live signed-in session.
/secrets/
